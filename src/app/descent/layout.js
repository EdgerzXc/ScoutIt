// A-096: distinct tab title (root template appends "· ScoutIt").
export const metadata = { title: "Descent" };

export default function DescentRouteLayout({ children }) {
  return <>{children}</>;
}
