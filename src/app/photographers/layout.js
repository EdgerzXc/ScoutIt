export const metadata = {
  title: "Photographers & Creators",
  description:
    "Discover photographers and spatial creators on ScoutIt — portfolios, bounty work, and the talent that captures every space.",
  alternates: { canonical: "/photographers" },
};

export default function PhotographersLayout({ children }) {
  return children;
}
