// A-096: distinct tab title (root template appends "· ScoutIt").
export const metadata = { title: "Profile" };

export default function ProfileRouteLayout({ children }) {
  return <>{children}</>;
}
