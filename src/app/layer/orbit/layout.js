export const metadata = {
  title: { absolute: "Orbit — Top-Ranked Spaces · ScoutIt" },
  description:
    "Orbit on ScoutIt — preview rankings of residential, commercial, and hospitality spaces across the Philippines, modeled by illustrative demand signals.",
  alternates: { canonical: "/layer/orbit" },
};

export default function OrbitLayout({ children }) {
  return children;
}
