export const metadata = {
  title: { absolute: "The Mantle — Discovery · ScoutIt" },
  description:
    "Mantle · Discovery on ScoutIt — curated discovery across the spatial intelligence layer.",
  alternates: { canonical: "/layer/mantle" },
};

export default function MantleLayout({ children }) {
  return children;
}
