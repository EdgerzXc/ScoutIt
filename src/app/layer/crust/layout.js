export const metadata = {
  title: { absolute: "The Crust — Verified Ecosystem · ScoutIt" },
  description:
    "Crust · Network on ScoutIt — verified brokers, photographers, researchers, and event planners who power the spatial ecosystem.",
  alternates: { canonical: "/layer/crust" },
};

export default function CrustLayout({ children }) {
  return children;
}
