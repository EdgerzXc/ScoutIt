export const metadata = {
  title: { absolute: "Metropolis — Explore Spaces · ScoutIt" },
  description:
    "Metropolis · Explore on ScoutIt — search the full property directory by category, location, and radius across six space categories.",
  alternates: { canonical: "/layer/metropolis" },
};

export default function MetropolisLayout({ children }) {
  return children;
}
