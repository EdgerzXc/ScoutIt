export const metadata = {
  title: "Brokers & Advisors",
  description:
    "Browse verified brokers and advisors on ScoutIt. Scout Rating is earned through verified closures only — never bought, never tier-granted.",
  alternates: { canonical: "/brokers" },
};

export default function BrokersLayout({ children }) {
  return children;
}
