// A-096: distinct tab title (root template appends "· ScoutIt").
export const metadata = { title: "Calendar" };

export default function CalendarRouteLayout({ children }) {
  return <>{children}</>;
}
