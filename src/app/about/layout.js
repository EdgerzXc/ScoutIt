export const metadata = {
  title: "About ScoutIt",
  description:
    "ScoutIt is a spatial intelligence and discovery platform for the Philippines, bringing property records, contextual signals, and the people around a space into one decision path.",
  alternates: { canonical: "/about" },
};

export default function AboutLayout({ children }) {
  return children;
}
