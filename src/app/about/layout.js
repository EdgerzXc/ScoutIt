export const metadata = {
  title: "About ScoutIt",
  description:
    "ScoutIt is a Spatial Commerce platform for the Philippines — Bloomberg for space. Intelligence first, transactions never. No guesswork, no gatekeeping, no pressure.",
  alternates: { canonical: "/about" },
};

export default function AboutLayout({ children }) {
  return children;
}
