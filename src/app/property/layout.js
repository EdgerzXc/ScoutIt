export const metadata = {
  title: "The Space Directory",
  description:
    "The full ScoutIt space directory — residential, commercial, short-term rental, hospitality, restaurants, and venues across the Philippines.",
  alternates: { canonical: "/property" },
};

export default function PropertyDirectoryLayout({ children }) {
  return children;
}
