export const metadata = {
  title: "Event Planners",
  description:
    "Find event planners on ScoutIt — the professionals who turn venues and spaces into experiences across the Philippines.",
  alternates: { canonical: "/event-planners" },
};

export default function EventPlannersLayout({ children }) {
  return children;
}
