export const metadata = {
  title: "Stories & Market Intel",
  description:
    "Editorial briefings and market signals from ScoutIt — neighborhood stories, category intel, and the data behind Philippine space.",
  alternates: { canonical: "/intel" },
};

export default function IntelLayout({ children }) {
  return children;
}
