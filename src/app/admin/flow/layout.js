// A-096: distinct tab title (root template appends "· ScoutIt").
export const metadata = { title: "Flow Map" };

export default function AdminFlowRouteLayout({ children }) {
  return <>{children}</>;
}
