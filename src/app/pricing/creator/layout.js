export const metadata = {
  title: { absolute: "Photographer & Creator Plans · ScoutIt" },
  description:
    "Plans for Photographers & Intel creators on ScoutIt — CDN portfolios, priority bounty routing, and Connects earned through verified work.",
  alternates: { canonical: "/pricing/creator" },
};

export default function CreatorPricingLayout({ children }) {
  return children;
}
