export const metadata = {
  title: { absolute: "Owner Plans · ScoutIt" },
  description:
    "Plans for Property Owners on ScoutIt — the VIP Spatial Vault, 3D capture, broker management, and white-glove portfolio curation.",
  alternates: { canonical: "/pricing/owner" },
};

export default function OwnerPricingLayout({ children }) {
  return children;
}
