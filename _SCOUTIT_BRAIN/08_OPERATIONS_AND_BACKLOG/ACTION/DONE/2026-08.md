---
section: "08_OPERATIONS_AND_BACKLOG/ACTION/DONE"
status: active
tags: [done, evidence, 2026-08]
updated: 2026-08-24
related: ["[[08_OPERATIONS_AND_BACKLOG/ACTION/DONE/README|Done Index]]", "[[00_MASTER_ACTION_PLAN]]"]
---

# Done — August 2026

| ID | Closed | Outcome | Evidence / boundary |
|---|---|---|---|
| D-2026-08-01 | 2026-08-18 | Showcase visual overhaul completed | Cosmic per-rank atmosphere, curated merit cards, briefing CTA, responsive layout; later owner-locked in scripts/approved-surfaces.json |
| D-2026-08-02 | 2026-08-21 | Showcase and parent navigation owner-approved | verify:surfaces protects ShowcaseStage.js; visual redesign requires new explicit approval |
| D-2026-08-03 | 2026-08-16 | Public contact route and delivery chain completed | /contact → Turnstile → API → Postgres → Resend → delivered inbox; retention remains active work |
| D-2026-08-04 | 2026-08-16 | Search Console verified and sitemap submitted | Domain property verified with historical data; production sitemap submitted; processing status remains a follow-up only |
| D-2026-08-05 | 2026-08-14 | Category authority defect closed | Obsolete MOCK_CATEGORIES removed; Airtable category authority guarded by categoryAuthority.test.js |
| D-2026-08-06 | 2026-08-14 | Bare React style-resource hazard closed | Focused scan records zero bare style resource blocks |
| D-2026-08-07 | 2026-08-20 | Universal header visual contract restored and verified | Glass/luxury presentation and responsive public pass accepted; functional menu completion remains A-001 |
| D-2026-08-08 | 2026-08-21 | Market article/property bridge reconciled | Location/property fields read truthfully; no fabricated Ridgeline briefing |
| D-2026-08-09 | 2026-08-22 | Mandatory action-documentation lifecycle established | Repository rules, every-turn SOP, Master Action Plan, and Action Rules now require one stable task ID, one queue home, same-turn disposition, and evidence-based Done closure; documentation-only diff and approved-surface lock verified; no product behavior or visual boundary changed |
| D-2026-08-10 | 2026-08-22 | U-001 Mission Control coordinates action repaired | mission-control/src/app/dashboard/coordinates/actions.js now declares the admin client before querying it in loadFlaggedCoordinates, keeping the staff AGENT tier guard and the real pipeline_status column; new test/coordinates-verification-action.test.mjs proves the ordering and guard (verified red against the reintroduced bug, then green); MC suite 47/47, eslint clean, next build clean, verify:surfaces passed. Boundary: staff coordinate verification is still unexercised against live Supabase data. |
| D-2026-08-11 | 2026-08-22 | U-002 Versioned Terms acceptance and truthful legal dates completed | Onboarding requires an explicit checkbox for the published version; /api/auth/complete-onboarding rejects missing/stale versions, writes append-only terms_acceptances evidence, aborts on failed persistence, and preserves the original accepted_at on re-submission; isOnboardingComplete now re-opens onboarding on a version change and never reads a NULL as consent; Terms/Privacy render the real version and effective date and no longer assert an unregistered entity, DPO office, NPC filing, or payment processor. Verified: 1383/1383 vitest, eslint clean, next build clean, verify:surfaces passed. **Correction 2026-08-22 (same day, during A-006): the migration is NOT unapplied. The live database already carries it as `20260821122706 versioned_terms_acceptance` — `terms_acceptances` exists with the exact columns, `user_profiles` has both columns, RLS is on with zero policies as designed. The deploy-ordering hazard recorded here does not exist and the O-004 note was corrected. Live-schema reconciliation also exposed a real defect this row missed, now closed as U-007. Legal wording still needs counsel under O-003.** |
| D-2026-08-12 | 2026-08-22 | U-003 Transparent Sample Intel disclosure completed | Every sample briefing carries the "Sample data — for human testing" label on the Intel index cards, the rotating featured hero, the article detail header and body notice, and the click-through side panel; real Airtable articles are flagged isSample:false and render no label. Verified in a real browser at 390 and 360 px: /intel showed 9-10 labels with zero clipping and no horizontal overflow, sample detail /intel/bgc-spatial-movement showed 2 labels, the side panel label sat fully inside the panel once the entrance animation was neutralised, and the live Airtable article /intel/test-intel showed none. sampleProtectionContract + mockArticles tests pass; verify:surfaces passed with no checksum change. Boundary: sample intel detail routes are still crawlable (no noindex) unlike sample property pages — recorded in the Inbox, not fixed here. |
| D-2026-08-13 | 2026-08-22 | U-004 Mobile universal-header menu target reconciled | The narrow-width menu control (and the <=340px display control) moved from 34-36px to 44px. Measured live at 320/360/390: rendered control 48x48 at every width, header left/centre/nav bands never overlap, document scroll width equals viewport width so no horizontal overflow, and real keyboard Tab focus shows the gold focus-visible ring on the button labelled "Menu". New src/lib/__tests__/headerTouchTarget.test.js fails when any header-menu-btn/header-eye-btn size drops below 44px (verified red, then green). masterCascadeNavigation + publicProfileIndexability tests pass, typography audit passes, eslint clean, verify:surfaces unchanged. Boundary: locked Showcase stage and command bar untouched; no header visual redesign. |
| D-2026-08-14 | 2026-08-22 | U-005 Mixed working tree split and verified | Owner authorised local commits on main (no push, no deploy). The tree was separated into six reviewed change sets committed in dependency order: CI visibility (45157c4), Mission Control assertTier guards and migration wrappers (31a96ee), U-001 coordinates repair (05f104a), U-002 versioned Terms acceptance (163f6de), U-003 sample Intel disclosure (9e7fc7d), U-004 header touch target (b5e793f), and this action-control/ledger change set. Whole-tree verification on the final state: 1385/1385 vitest across 123 files, Mission Control 47/47, eslint clean for src and mission-control/src, both Next production builds clean, typography audit clean, verify:surfaces passed with no checksum change. Boundary: nothing pushed, nothing merged, nothing deployed; the U-002 migration remains unapplied and gated by O-004. |
| D-2026-08-15 | 2026-08-22 | A-001 Universal navigation operating layer completed | Menu entries now come from one canonical manifest (src/lib/navigationManifest.js) instead of hand-written links; a test proves every destination resolves to a route in the build. Added Escape dismissal with focus returned to the trigger, Tab/Shift+Tab containment inside the open panel, outside dismissal on pointerdown so a touch tap closes on the same gesture, closure on any route change including browser Back, aria-expanded/aria-controls/aria-haspopup/role=menu semantics, and aria-current on the active page. Mobile bottom sheet and desktop popover already existed and were left visually unchanged; the sheet max-height moved from vh to dvh. Verified live at 390 and 1280: open focuses the first item, Escape restores focus to the control labelled Menu, a touch-type pointerdown outside closes, browser Back closes without any menu click, Tab wraps last-to-first and Shift+Tab first-to-last, items render block-level at 197px on desktop and >=49px tall full-width in the sheet, no horizontal overflow, no app console errors. Caught and fixed a regression introduced during this work: styled-jsx does not scope elements rendered from a map callback, so `.header-dropdown a` rules stopped matching and every item silently lost its padding, colour and 44px mobile height — selectors moved to `:global(a)` and a test now fails if the scoped form returns. 1396/1396 vitest, eslint clean, typography audit clean, production build clean, verify:surfaces unchanged. Boundary: "server-approved role states" needed no menu behavior because the menu exposes no role-scoped destination and the account row is a display hint with server-side enforcement at the route; production-mode and authenticated-surface coverage remains A-005. |
| D-2026-08-16 | 2026-08-22 | U-006 The promised 7-day chat purge now has a working implementation | Discovered while verifying A-002 that ScoutIt promised a purge it could not perform: `purge_expired_chat_messages()` was live in Supabase but had no caller anywhere, pg_cron was not installed, and the function updated `deal_messages.content` while the live column is `body` — it would have raised 42703 on first execution (Standing Rule 20). Replaced with one implementation that has a named consumer: src/lib/chatRetention.js plus /api/cron/purge-chat-messages, cron-authorised, written against the live column names, redacting only closed deals past a seven-day cutoff with no open_hold/under_review dispute; registered daily in vercel.json; migration 20260822000001 drops the dead function so one rule keeps one definition. Verified: route actually executed locally — 401 unauthorised, 200 authorised returning {purged:0, eligibleDeals:0, retentionDays:7} against the real database (0 deals, 0 messages, so no production data was touched); 12 tests including a mocked-client behavioural suite proving the dispute exemption, the empty cases, the auth refusal and the read-failure path, verified red when the exemption was removed. 1408/1408 vitest, eslint clean, production build clean, verify:surfaces unchanged. Boundary: migration unapplied and the schedule inactive until deployed — both owner-gated; the Privacy section 04 claim only becomes true on deploy, which is safe today because no chat data exists. |
| D-2026-08-17 | 2026-08-22 | A-004 CI failure visibility completed | .github/workflows/ci.yml now ends in a `required-ci-result` job that runs `if: always()`, needs `verify`, and exits non-zero for any result other than success — so a cancelled or failed verification can no longer read as green. The verify job runs the approved-surface lock, lint, the typography regression audit, the unit suite, the Mission Control suite, browser-suite discovery, and the placeholder-env build. Browser discovery is `playwright test --list`, which enumerates and executes nothing: confirmed locally, 466 tests across 25 files listed with no run and no production data touched. Added the Mission Control install/test steps in this pass because its 47 tests, including the U-001 regression guard added today, were not executed by any workflow. Verified: workflow parses as valid YAML with both jobs and the gate correctly wired; `npm ci --prefix mission-control` and the Mission Control suite both succeed locally (47/47). Boundary: the gate logic has not been observed going red in a real GitHub Actions run — the next pushed branch is the first live proof; nothing has been pushed. |
| D-2026-08-18 | 2026-08-22 | A-003 Preview-deployment verification contract chosen and implemented | Chose production-after-merge over protected previews because it needs no credential; a Vercel automation bypass token remains available as an owner action, not an assumption. playwright.config.js accepts SCOUTIT_E2E_BASE_URL and skips the local build/server when a deployed target is supplied. Added assertScoutItRendered() to e2e_tests/full-system/helpers.js — anchored on div.grain and the Organization JSON-LD from the root layout, with explicit detection of Vercel/Cloudflare interstitial wording — and wired it into expectRealContent() so every spec using the shared helper is covered. Proved both directions: against a stand-in protected-preview server every public route failed with "Render anchor failed: the response is an interstitial, not ScoutIt", where the previous body-text probe would have passed all of them; against the real production build the same spec file ran 23/23 green. Contract written to _SCOUTIT_BRAIN/08_OPERATIONS_AND_BACKLOG/VERIFICATION_CONTRACT.md. Boundary: no deployed ScoutIt host has been measured yet — the method is ready, the first real run happens after an owner-approved deploy; the suite stays read-only against live data. |
| D-2026-08-19 | 2026-08-22 | U-007 Re-consent no longer demotes an existing account | Found by reconciling U-002 against live data during A-006: 5 of 15 profiles are onboarded and none carried a current terms acceptance, so the new version gate would send every one of them back through onboarding — where the completion route ran full first-time provisioning. For the founder account (`active_roles: broker/owner/buyer/provider`, `role: admin`, `subscription_tier: universe`) that would have written `active_roles: ["buyer"]`, `role: "seeker"` and `subscription_tier: "starry"` over it, silently, behind a consent checkbox. The route now detects an account with `onboarding_completed_at`, records the acceptance, updates only `terms_accepted_at` and `terms_version`, and returns the preserved roles without touching wallets, tier, or role. Verified: two new tests built from the real live profile shape, both proven red when the guard is bypassed; 14/14 in the onboarding suite; eslint clean. Boundary: no live row was written during this work — the defect was found by reading the schema and profile shapes, not by exercising an account. |
| D-2026-08-20 | 2026-08-22 | A-006 Security, grant and migration reconciliation completed (read-only) | Read the live database directly rather than the documents describing it. RLS: every public table has it enabled except `spatial_ref_sys` (PostGIS system table, already held under O-004). Policies: no always-true policy grants a write to anon or authenticated — the ten `true` expressions are SELECT-only on deliberately public tables (badge_definitions, broker/researcher profiles, feature_flags, property_slug_history, user_availability) plus service_role full access on the two intel tables. SECURITY DEFINER: all 18 ScoutIt-owned functions are executable only by postgres and service_role, so Standing Rule 8 holds across the board; the only PUBLIC-executable definers are PostGIS `st_estimatedextent` overloads owned by supabase_admin. Migrations: 69 applied, and the reconciliation corrected a false record — `versioned_terms_acceptance` is applied, not pending (see the correction on D-2026-08-11). Migration-wrapper tests pass (Mission Control 47/47) and now run in CI. The pass also surfaced two real defects closed the same day as U-006 and U-007. Boundary: read-only throughout — no migration applied, no policy changed, no row written; `spatial_ref_sys` and the two unapplied migrations remain owner-gated; `user_availability` being world-readable (user_id, timezone, weekly_schedule) is recorded as intended booking behaviour, not audited as a decision. |
| D-2026-08-21 | 2026-08-22 | A-005 Production-mode verification completed, and the suite was found to be measuring nothing | Final production-build run: **472 passed, 2 skipped, 0 failed, 0 flaky**, Playwright exit 0, read from the JSON reporter across desktop Chrome and Pixel 5. Getting there required fixing what the run exposed. **A first run appeared green at "392 passed, exit 0" — the exit code came from a `tail` pipe, not Playwright; the JSON reporter showed 82 failures.** Causes: 56 were Vercel speed-insights 404ing on a local build and 1 was Google Identity refusing localhost as an origin (both narrowly ignored as environment, patterns that cannot mask an app error); 17 were the A-003 render anchor demanding exactly one `div.grain` when the homepage has two; and the rest were **`signInAsMock` writing the identity to `scoutit_user` while the dashboard authoritatively reads `scoutit_dev_user` — every authenticated dashboard test had been running signed-out**. Stale fixtures revealed once sign-in worked: a "Get Started" button that exists nowhere in the app (real copy: "Add your first property" / "Add Property →"), changed wizard copy, an ambiguous "Invite Member" locator, and a "connect with an authorized broker" button that has never existed under that name. All re-aimed, none deleted. Coverage now genuinely spans public, property, profile, onboarding and authenticated dashboard states on mobile touch and desktop keyboard. Boundary: still a localhost production build — no deployed host has been measured (that is the A-003 method, first usable after an owner-approved deploy). |
| D-2026-08-22 | 2026-08-22 | A-001 follow-up: two menu defects found only by production-mode testing | The A-001 menu passed manual browser checks because those checks disabled animations. Under a real production build: (1) focus-on-open never landed — the panel transitioned `visibility`, and `focus()` on a still-hidden element is a silent no-op, so the menu opened with focus left on the page underneath; `visibility` is now switched discretely (instant on open, delayed 180ms on close, appearance unchanged) plus a rAF guard. (2) The route-change close effect also ran on mount, cancelling a menu the user had just opened — a race visible only on a slower device; it is now guarded on an actual pathname change. Also corrected the ARIA: `role="menu"`/`role="menuitem"` promised arrow-key navigation that does not exist and stripped native link/button roles (it broke an unrelated display-settings test), so the trigger is now a plain disclosure with aria-expanded/aria-controls. Verified: 30/30 across the header and pilot-auth specs on both projects, then 472/472 in the full suite. |
| D-2026-08-23 | 2026-08-23 | A-008 Sample Intel detail indexability reconciled | src/app/intel/[article-slug]/page.js generateMetadata now emits `robots: { index: false, follow: true }` when `isSample: true`, aligning with sample property and unit detail routes; live Airtable editorial articles (isSample: false) retain standard indexability. Verified: sampleProtectionContract test updated and passing (5/5), 1410/1410 Vitest, MC suite 47/47, eslint clean, typography audit clean, verify:surfaces passed with no checksum change. Boundary: metadata directive only; visual badges and on-site discovery unchanged. |
| D-2026-08-24 | 2026-08-23 | A-007 / O-007 Showcase sample data disclosure and honest merit curation | Owner authorized sample disclosure and metric honest relabelling on locked surface. src/components/board/ShowcaseStage.js now renders `Sample data — for human testing` in the top viewport bezel, replaces the ungrounded "Verified" badge with `Curated Merit` / `Human-Curated Architectural Merits`, and labels demand telemetry as projected sample data. Surface checksum updated in scripts/approved-surfaces.json and verified (3/3). Verified: sampleProtectionContract test updated and passing (6/6), 1410/1410 Vitest, MC suite 47/47, eslint clean, typography audit clean, verify:surfaces passed cleanly. Boundary: visual stage and checksum refreshed under explicit owner authorization; non-destructive to animations and shaders. |
| D-2026-08-25 | 2026-08-23 | A-009 Codex–Antigravity shared workflow documentation accepted | The canonical Action Rules, Master Action Plan, and agent workspace now durably define `CURRENT_TASK → ANTIGRAVITY_REPORT → CODEX_REVIEW → one correction or accept`, permanent role/skill ownership, one-correction maximum, Codex fallback repair, sole-backlog authority, and Codex-only closure. Antigravity correction 1 reconciled Search Console and human-testing-readiness state; Codex independently read the real scoped diff, confirmed A-009 had exactly one queue home and protocol link targets resolved, ran scoped `git diff --check`, and reran `npm run verify:surfaces` successfully (3 surfaces). Boundary: documentation/workflow control only; no commit, push, deploy, migration, live mutation, product code, or checksum change by A-009. |
| D-2026-08-26 | 2026-08-23 | A-010 owner-cancelled; Antigravity workflow retired | Owner stopped the worker and directed Codex to work directly. A-010 was removed from Active before implementation; no lease helper, dispatcher, product code, UI, migration, external mutation, commit, push, or deployment was produced. Canonical Master Action Plan and Rules now define direct Codex execution; the former agent workspace is explicitly archived as non-executable historical evidence. Boundary: prior A-009 evidence is preserved as history and grants no current execution authority. |
| F-007 | 2026-08-23 | Crust professional-category explanation and routing experience completed | `/layer/crust` now gives Advisors, Photography, Research, and Event Design distinct dossiers covering role, named evidence checked, engagement timing, ScoutIt boundary, truthful live/sample/empty roster state, and relevant next actions. URL-backed tabs support Arrow/Home/End keys, focus, history, mobile layout, and reduced-motion/non-WebGL operation. Verified in Chromium and Mobile Chrome: dedicated Playwright 8/8 including 320/390/768/1440 containment; focused contract 4/4; full repository gate 133/133 files and 1445/1445 unit tests; 482 E2E cases discovered; lint, typography, and approved-surface lock 3/3 passed. Boundary: ScoutIt reports only stored evidence and availability; it does not credential, employ, broker, guarantee, or endorse providers, and unavailable tools remain waitlisted. No checksum, migration, deploy, or live-data write. |
| F-003 | 2026-08-23 | Orbit luxury restraint and truthful preview methodology completed | `/layer/orbit` now describes a curated demand-index preview and sample model instead of claiming live verified inquiry volume, a rolling 30-day calculation, 100+ ranked properties, or manufactured velocity. Clashing gradients/rapid pulse were replaced with tokenized black/gold restraint, responsive hierarchy, and reduced-motion guards. Source path `/api/showcase` → `getCmsBundle` → `getShowcaseEntries` was independently reconciled; desktop/mobile Orbit and locked Showcase were visually reviewed. Orbit/sample contracts and the current full gate pass; no F-003 checksum, migration, live-data write, deploy, commit, or push occurred. |
| F-004 | 2026-08-23 | Discover regional journey and content ordering completed | `/discover` now keeps one authoritative region across News Feed → Regions → Spaces, resets stale region state on category/URL changes, reports honest counts/empty states, and keeps space-card spotlight independent from region selection. The duplicate mobile rail logo discovered during the later F-006 visual pass was also removed in favor of the single global identity. Focused region contracts pass; desktop and Pixel 5 renders were reviewed; the current full 1,472-test/build/lint gate passes. Boundary: no locked surface, checksum, live data, migration, deploy, commit, or push changed. |
| F-005 | 2026-08-23 | Space Directory product and retrieval audit completed | Canonical audit now matches current `/layer/metropolis` and `/property` code, Dual-CMS authority, anonymous premium stripping, structured category/location/price-band/radius authority, null-safe client search, and F-001's server-only local retrieval coordinator. A false claim that `/api/search` and `/api/intel/search` already existed was removed; both remain future/owner-gated. Public candidates must resolve to current approved canonical sources and cannot expose internal chunks or invent properties. Directory/retrieval focused contracts pass 11/11 within a combined 26/26 closure suite; current production build, lint, and full 1,472-test gate pass. Boundary: no semantic endpoint/provider/migration, locked-surface edit, deploy, commit, or push. |
| A-011 | 2026-08-23 | Action router reconciled after the F-series closure | Canonical Start Here, SOP, Master Action Plan, Urgent, and Future records now agree that A-007 is completed, F-002's local foundation is complete with real-source/schema activation owner-gated, and Active is empty. Showcase descriptions now preserve explicit sample/projected labels and source-evidenced curation instead of linking to nonexistent work. Scoped searches find no stale A-007 Active anchor or old F-002 promotion claim; A-011 has no live queue duplicate; approved-surface lock passes 3/3 and `git diff --check` reports no errors. Boundary: documentation routing only; no product surface, checksum, owner gate, migration, live data, deploy, commit, or push changed. |
| U-008 | 2026-08-23 | Stored XSS through unescaped JSON-LD closed | `JSON.stringify` output was passed to `dangerouslySetInnerHTML` inside `<script type="application/ld+json">` at `src/app/property/[id]/page.js`, `src/app/hubs/[slug]/page.js`, and both sinks in `src/components/seo/JsonLd.js`. `JSON.stringify` does not escape `<`, so an owner-supplied listing title containing a closing script tag terminated the block and the remainder parsed as markup. New `src/lib/jsonLdScript.js` escapes `<`, `>`, `&`, U+2028 and U+2029 to their `\uXXXX` JSON forms at the SINK — chosen over escaping in the builder because `mergeFaqIntoOverride` returns an operator's hand-written override untouched on several paths. Escapes are valid JSON string escapes, so `JSON.parse` returns identical data and structured-data output is unchanged. 13 tests in `jsonLdEscaping.test.js` written RED first: 6 behavioural, plus source guards asserting every sink imports the escaper and no sink contains `__html: JSON.stringify(`. Verified: full 1,513-test suite, `npm run build` compiled successfully, ESLint clean, approved-surface lock 3/3. Boundary: no commit, push, deploy, migration, checksum change, or live-data write. |
| U-009 | 2026-08-23 | Airtable formula injection in `/api/geo-pricing` closed | The unauthenticated POST interpolated the caller's `category` straight into `filterByFormula=AND(Approved_For_ScoutIt=TRUE(), LOWER(SpaceCategory)=LOWER('${category}'))`. A single quote closed the literal and allowed the filter — including the `Approved_For_ScoutIt=TRUE()` condition separating public listings from withheld rows — to be rewritten by any caller. The if-chain that fell through to `Listed_Price` for unknown input is now a frozen `CATEGORY_PRICE_FIELD` allowlist; an unrecognised category returns 400. The formula is built from the allowlist's own key, never from the wire, and is `encodeURIComponent`-wrapped. Price and location are validated before the first paid call, so a rejected request now spends neither a Mapbox geocode nor an Airtable read. 6 tests in `geoPricingApi.test.js` written RED first — the RED run confirmed the injection payload returned 200 and reached Airtable. Boundary: no commit, push, deploy, migration, or live-data write. |
| U-010 | 2026-08-23 | `/api/reactions` unauthenticated unbounded write closed, and the UI stopped claiming false success | The route accepted an unauthenticated POST and wrote to a record-capped, billed Airtable base with no rate limit, no `reaction_type` allowlist, no length caps and no id validation, then swallowed every error and returned `{ ok: true }` unconditionally. It now meters 30/minute per client IP through the existing `src/lib/rateLimit.js`, validates `property_id` against a bounded safe-character rule, checks `reaction_type` against an allowlist, caps label fields, calls Airtable through `fetchWithRetry` (which does not blind-retry a POST), returns 502 on a failed write without forwarding upstream error text, and 503 when unconfigured. The allowlist is `Save`, `Inspired Me`, `Potential Fit`, `Interested` — the real keys of `REACTION_SHAPES`; a first draft invented its own vocabulary and a drift-guard test that reads `ReactionButtons.js` and `BottomNav.js` caught it before it shipped. Client half: `ReactionButtons.js` called `setShowConfirm(true)` on every path and always rendered "Saved to Your Board.", including on removal and after a thrown `localStorage.setItem`. New `reactionFeedback.js` state table distinguishes saved / removed / storage-failed / sync-failed; a failed anonymous ping stays deliberately silent (the board is the localStorage entry — if that landed, the user's action succeeded) and is reported to Sentry via `reportError` instead of an empty catch. 9 route tests plus 5 feedback tests written RED first. Boundary: no commit, push, deploy, migration, or live-data write; `BottomNav.js` still swallows its own ping failure and is untouched. |
| U-010b | 2026-08-23 | Reaction tile motion and touch behaviour corrected | Same-file design pass taken with the fixes above, applied against Emil Kowalski's design-engineering checklist. Three `transition: all` rules (which were tweening `stroke-width`, `fill` and a `drop-shadow` filter) became named-property transitions; `transform: scale(0.92) !important` became `scale(0.96)` with the `!important` removed after confirming it contended with nothing (hover targets the child `.shape-wrapper`, press targets the tile); hover states are now gated behind `@media (hover: hover) and (pointer: fine)` so a tap on a touch screen no longer leaves a tile stuck in the gold hover glow reading as selected; a `prefers-reduced-motion` block removes travel while keeping the colour and opacity changes that carry state; the status line reserves its row with `min-height` to stop layout shift, drops the untweenable `visibility` transition, exits faster (160ms) than it enters (220ms), and gained `role="status"` / `aria-live="polite"` now that it reports state rather than decorating. Boundary: `ReactionButtons.js` is not a checksum-locked surface (`verify:surfaces` covers 3 surfaces, none of them this file) and the lock passed before and after; no owner-approved surface changed. |
| A-012 | 2026-08-24 | Spend and write ceilings applied to the unauthenticated routes | `src/lib/rateLimit.js` guarded 2 of 95 routes. It now also meters `/api/ai/promote` (10/min -- anonymous by design while `pre_launch_free_mode` is on, and every call spends Gemini tokens), `/api/geo-pricing` (20/min -- one Mapbox geocode plus one Airtable read per accepted call), and `/api/analytics` (120/min -- deliberately high so honest telemetry from a real reader is never metered into silence). Each meters before parsing the body and returns 429 with `Retry-After`. A new `src/lib/clientIp.js` replaces three divergent hand-rolled copies of the same header walk and documents why the fallback is a constant rather than null (a null key puts every unidentifiable caller in one shared bucket). `/api/reactions` was refactored onto it. **Scope correction recorded in the same change:** the original A-012 text claimed `/api/ai/*` and the inquiry paths were unauthenticated; a route-by-route re-read disproved that -- `assimilate`, `blueprint`, `rewrite`, `counter-offer` all 401 anonymous callers, `/api/inquiries`, `/api/waitlist` and `/api/contact` are Turnstile-guarded with zod schemas, and the cron routes use `authorizeCronRequest`. 5 tests written RED first (the RED run showed 60 consecutive unmetered 200s on the Gemini route). **Verified against a running server, not only mocks:** 10 requests returned 200 and the 11th returned `HTTP 429` with `retry-after: 46` and `cache-control: private, no-store`; a request from a different IP still returned 200, confirming buckets are per-caller. Boundary: the limiter remains per-instance (Vercel gives each instance its own counters); Upstash is a dependency but moving to a distributed counter was explicitly NOT done and needs a measured reason. No commit, push, or deploy. |
| A-013 | 2026-08-24 | Third-party calls that could hang are now bounded | `fetchWithRetry` (attempt timeout, total budget, circuit breaker, and no blind retry of a POST) existed and was used by `airtable.js`, `cmsCache.js` and the mapbox route, while other call sites used a bare `fetch` with no timeout at all. The failure mode is not a dead upstream -- that returns fast and is handled -- but a SLOW one, where the request holds open until the platform kills the function and the user sees a hang instead of an error. Converted: `/api/geo-pricing` (Mapbox + Airtable, each with a named circuit), `/api/health` (single short attempt, `retries: 0` -- retrying would let a health probe report a slow upstream as healthy), `/api/intel/ingest`, `/api/dashboard/verify-freshness` (both call sites), and `src/lib/intelPublish.js` whose injectable `fetchImpl` seam kept its default as the unbounded client, so every production caller silently got no timeout. **Two scope corrections against my own list:** `src/lib/isochrone.js` was listed as a defect but already wraps its call in an AbortController with `REQUEST_TIMEOUT_MS` -- it is bounded, was never the bug, and a test now pins that fact rather than converting it; `intelPublish.js` had no literal bare `fetch(` call because the unbounded default was a parameter. 12 tests written RED first, including comment-stripped source guards so prose about the old behaviour cannot mask a regression. Boundary: no commit, push, or deploy. |
| A-014 | 2026-08-24 | AI Promote degrades to a real answer instead of a dead end | `PromoteModal.js` turned every non-OK response into one red error block and stopped, while `buildPromoPack()` in `src/lib/shareBriefing.js` could produce the same three formats from the listing's own recorded facts with no AI call, no network and no tier resolution -- `ShareModal.js:189` already called it client-side, so this was reuse of a proven path rather than new capability. Every failure path now degrades to the local pack, and `source` stays truthful (`factsheet`, never `ai`) so the header never presents deterministic template copy as the AI draft. New `promoteFallback.js` separates the two failures that deserve different answers: a 429 knows when it can be retried and says so in words, everything else does not and refuses to invent a schedule; a missing `Retry-After` renders as "in a moment" rather than as a fabricated number. The dead `error` state and its `sanitizeError` import were removed rather than left unrendered. 10 tests written RED first, including wiring guards asserting the modal actually reads `Retry-After` and no longer throws on a non-OK response. Boundary: the degraded advisory has not been seen in a browser -- the pure decision logic and the wiring are tested, the rendered state is not, and it belongs to the existing W-004 real-device pass. No commit, push, or deploy. |
| A-014b | 2026-08-24 | Promote advisory and retry control designed rather than defaulted | Design-engineering pass over the same surface, continuing U-010b. The failure block was `bg-error/10 border-error text-error` -- a red alarm that REPLACED the content. It is now a muted gold advisory that sits ABOVE working copy, because the local pack below is a real answer rather than a consolation, and on a black-and-gold page a red panel is the only cool-shifted element and reads as a bigger emergency than a busy AI service. Entry is `motion-safe:animate-[fadeIn_200ms_cubic-bezier(0.23,1,0.32,1)]` -- a real ease-out curve, under the 300ms UI ceiling, and gated by `motion-safe` so reduced-motion users get the content without the movement. The retry control specifies its transitioned properties instead of `transition-all`, presses to `active:scale-[0.97]`, and carries a visible `focus-visible` ring. It is rendered ONLY for `rate-limited`, because a retry button on a genuinely unavailable service is a button that exists to disappoint. `role="status"` and `aria-live="polite"` were added since the block now reports state rather than decorating. Boundary: not a checksum-locked surface; `verify:surfaces` passed 3/3 before and after. |
| A-015 | 2026-08-24 | Environment contract documented and drift-proofed | `.env.example` documented 12 keys while the application read 44. Thirty-one were required-but-undocumented, including `SUPABASE_SERVICE_ROLE_KEY`, `CRON_SECRET`, `TURNSTILE_SECRET`, `IP_SALT`, `WISHLIST_SHARE_SECRET` and `AIRTABLE_REACTIONS_TABLE_ID` -- the last of which is exactly how O-010 stayed invisible. The template now documents every variable the code reads, each marked `[required]` or `[optional]`, each with a line saying what breaks in its absence and why (URL-restricted vs unrestricted Mapbox tokens, why the anon key is public but the service-role key bypasses RLS, why `GEMINI_MODEL` must never be hardcoded). `CALENDLY_PERSONAL_TOKEN` was removed: it was documented but read nowhere in the application, and the Calendly integration is parked. 4 tests written RED first derive the required list from source at test time and fail on drift in either direction, plus a check that the template contains no value shaped like a real JWT, Resend key, Airtable PAT or Mapbox secret. Scanner covers `src/` plus the root-level Sentry and instrumentation configs, which is where `NEXT_PUBLIC_SENTRY_DSN` is read. **No secret values were read or written**: the template holds placeholders only and `.env.local` was never opened. Boundary: documenting a variable does not set it -- whether the values exist in Vercel remains owner check O-010. |
| A-016 | 2026-08-24 | The shared submit button for paid actions corrected | `ImpeccableButton` is the control behind "Spend 1 Connect" in three modals. Five defects, all fixed. (1) The spinner REPLACED the children, so a ~200px button collapsed to ~16px the instant it was pressed -- a visible jolt on every send. The label now stays mounted at `opacity-0` inside a normal-flow span while the spinner overlays absolutely, so the width never changes. (2) That same swap removed the accessible name; a screen-reader user reaching the button mid-submit heard an unnamed button. The label now persists and `aria-busy` reflects the loading state, with the spinner `aria-hidden`. (3) `disabled={isLoading \|\| props.disabled}` was written BEFORE `{...props}`; JSX resolves duplicate props left-to-right, so any caller passing `disabled` silently defeated the double-submit guard on a paid action. The spread now comes first and `disabled` cannot be overridden. (4) `transition-all` became a named property list. (5) `animate-spin` became `motion-safe:animate-spin`. 7 tests written RED first. **Verified in the running app:** the live button reports `transition-property: transform, background-color, border-color, opacity`, sits at `position: relative`, and renders its label as a `position: static` child -- the exact structure that prevents the collapse. Boundary: these are source-level assertions because this repository writes JSX in `.js` files and the Vite/Rolldown pipeline vitest runs on only parses `.jsx`/`.tsx`; an esbuild loader does not help because Rolldown ignores that option, so no component in this repo can currently be render-tested. That limitation is recorded rather than worked around with a build-config change nobody asked for. |
| A-017 | 2026-08-24 | Double-submit guard moved next to the money | The three paid submit paths depended entirely on `ImpeccableButton` staying correct; `handleSubmit` itself had no re-entry guard and would spend twice if reached twice. Each handler now holds a synchronous `useRef(false)` latch checked and set before the status write, and released in a `finally` so a failed attempt can still be retried. **Deliberately a ref, not `if (status === "submitting")`:** `status` is React state and is stale within a tick, so two clicks dispatched before the next render would both read `"composing"` and both proceed -- a state guard would look correct in review and fail under exactly the double-click it exists to prevent. 12 tests written RED first (4 assertions across 3 modals), pinning the ref specifically and asserting the guard precedes the status write and is released in `finally`. **Explicitly out of scope:** durable server-side idempotency. The real fix for a retried or duplicated request is a uniqueness constraint in Postgres, and applying migrations is owner-gated in W-003. An in-process or Redis-TTL dedupe would look like a guarantee while being neither, and this backlog already has enough of those. Recorded against W-003 instead of half-built. |
| O-009 | 2026-08-24 | Reviewed security and stabilization release pushed to GitHub main | The owner authorized the release. The complete pending diff was reviewed, `git diff --check` was cleaned, and local `main` was pushed from `1daddbb` through `bab4a06`; post-push `HEAD` and `origin/main` both resolved to `bab4a069f3ce512e4f02b818e550b3da050bf1c0`. Before push, approved surfaces passed 3/3, ESLint passed, typography passed across 489 files, unit tests passed 1,563/1,563, Playwright discovered 518 cases in 28 files, and the Next 16.3 production build generated 121 pages. Boundary: this proves GitHub delivery, not Vercel completion; the subsequent live audit owns deployment and production-behavior verification. |

> **A-014 verification correction — 2026-08-24:** independent rerun of the canonical gate found the retry control at `text-[11px]`, below ScoutIt's 12px interface-text floor. It now uses `text-xs`; focused Promote tests pass 10/10, the full gate passes 1,563/1,563 unit tests with 518 E2E cases discovered, lint, typography, and surface locks 3/3, and the production build completes. The correction is included in pushed release `bab4a06`; W-004 retains real-device acceptance.
