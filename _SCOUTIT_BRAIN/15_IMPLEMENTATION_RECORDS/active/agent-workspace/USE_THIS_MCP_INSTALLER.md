# Use this Antigravity MCP installer

Run `install-antigravity-mcp-pack-safe.ps1` for future installs or repairs:

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "C:\Users\jerze\ScoutIt\_SCOUTIT_BRAIN\15_IMPLEMENTATION_RECORDS\active\agent-workspace\install-antigravity-mcp-pack-safe.ps1"
```

It installs the official MCP pack and then applies the OAuth compatibility correction required
by the currently installed Antigravity runtime. Do not run the older pack installer alone.
