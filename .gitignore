# Dependencies
/node_modules/
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Test and audit output
/coverage/
/test-results/
/playwright-report/
/.lighthouseci/
/audit_report.json
/lighthouse-report.json
/error-overlay-*.png

# Next.js and production output
/.next/
/out/
/build/

# Local and generated files
.DS_Store
*.pem
*.tsbuildinfo
next-env.d.ts
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.aider*

# Environment and deployment state
.env*
# ...but the template MUST be tracked -- being committed is its entire purpose.
# Without this negation a fresh clone gets zero environment documentation, which
# is how 31 required variables went undocumented (A-015). Real .env files above
# stay ignored; verify with: git check-ignore -v .env.local
!.env.example
.vercel/

# Reference assets and local tool state
/reference/
/.claude/
/.ua/
/.agents/
/awesome-legal-skills-temp/
/awesome-llm-apps/
/council-of-high-intelligence/
/.gemini/antigravity-ide/brain/
/.codex-tmp/



# Archived/superseded local review material
/_REVIEW_QUEUE/

# One-off generated repair/debug scripts
/fix_*.js
/inject*.js
/add_*.js
/execute.js
/extract_*.js
/final_*.js
/perfect_fix.js
/recover_*.js
/strip_and_link.js
/debug-*.html
/scratch_debug*.js

# Scratch workspace and captures
/shots/
/find-clicks*.js
/scratch/
/scratch_*.png
/scratch_*.js
# Agent review captures left at the repo root (e.g. .codex-profile-review.png).
# The file stays on disk for whoever wants it; it just is not repo content.
/.codex-*.png

# O-021 decided 2026-09-09 (option 2): the Brain is tracked by default.
# Only genuinely local things stay ignored: the personal daily log, vendored
# external tools, tool state, owner-supplied external inputs, and the two
# September audits below. Those two contain a working step-by-step
# reproduction of U-022 -- fine in a private repo, not in a public one -- and
# stay ignored until O-019 lands, per the owner's standing call. Secrets scan
# 2026-09-09 found no keys/tokens/private keys anywhere in the Brain, but
# staging/committing remains a separate owner decision: this edit only makes
# files trackable, it tracks nothing.
# ── THE VAULT IS NOT PUBLISHED (owner decision, 2026-09-10) ──────────
#
# The repository is PUBLIC and GitHub is used only as a place to push code.
# The vault is working material: operational ledgers, architecture internals,
# security status, audit findings. None of it needs to be on the internet.
#
# WHY THE OLD LIST WAS NOT ENOUGH, and this is the lesson worth keeping:
# it named a few folders and two audit files, and it looked like protection.
# **.gitignore does not untrack what is already tracked**, so 126 vault files
# had been public since they were first committed. They were removed from
# tracking on 2026-09-10 with `git rm --cached` — which leaves every file on
# disk and only stops publishing them.
#
# Everything under the vault is ignored, with ONE deliberate exception below.
/_SCOUTIT_BRAIN/*

# The exception: a test reads this file. `truthfulVoiceContract.test.js` asserts
# the canonical Voice & Copy Guide exists and still contains the banned-cliché
# table, so CI needs it present in a fresh clone. It is brand guidance — no
# credentials, no security posture, nothing an attacker gains from. Unignoring a
# file inside an ignored directory requires unignoring each parent first, which
# is what these three lines do.
!/_SCOUTIT_BRAIN/01_IDENTITY_AND_VISION/
/_SCOUTIT_BRAIN/01_IDENTITY_AND_VISION/*
!/_SCOUTIT_BRAIN/01_IDENTITY_AND_VISION/VOICE_AND_COPY_GUIDE.md

# FUSE artifacts left by in-place edits over a mounted filesystem.
# Not source; they are stale open handles to replaced files.
.fuse_hidden*

# A-089, 2026-09-09 triage. Lab clutter at the repo root is local experiment
# material, not review content: manifesto cinematic lookdev, one-off transition
# runner, dev-server log. Ignored with reason recorded here; files stay on disk.
# Migrations 20260831*/20260904*/20260905* + DONE/2026-09 + A-083/A-085 plans
# are trackable (not ignored) — content unchanged. Staging/committing them is a
# separate owner decision; this edit only makes lab noise ignorable.
# CRLF: .gitattributes already enforces LF (* text=auto eol=lf). The 60+ file
# warnings are expected normalization noise until the owner approves the
# one-time `git add --renormalize .` commit named in .gitattributes.
/manifesto-lab/
/apply-transitions.mjs
/lab-server.log
# Tool-cache churn is not review content. The already-tracked
# mission-control/.impeccable/hook.cache.json keeps showing modified until the
# owner approves `git rm --cached` for it; ignore future cache copies only.
**/.impeccable/hook.cache.json
